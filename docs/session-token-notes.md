# Session Token Notes

This middleware is intentionally small so PullSight can review a security-sensitive diff.

Follow-up work:

- Replace fallback test values with required environment variables.
- Use a constant-time comparison helper.
- Add tests for missing headers and invalid tokens.
